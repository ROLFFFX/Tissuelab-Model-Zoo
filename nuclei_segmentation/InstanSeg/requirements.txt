setuptools
numpy==1.26.4
matplotlib>=3.7.0
pandas>=2.0.0
opencv-python==4.8.0.74
scipy>=1.11.0
tiffslide==2.5.1
multiprocess
scikit-learn>=1.3.0
scikit-image>=0.21.0
tqdm
natsort==8.2.0
colorama>=0.4.6  # For better Unicode support on Windows
einops>=0.7.0  # Required for transformers

# InstanSeg dependencies
instanseg-torch==0.0.9

# Dependencies for embedding generation (optional, reuse StarDist's embedding):
sse-starlette
fastapi
uvicorn
transformers
tissuelab_sdk==0.1.12
czifile
pydicom
torch
zarr

# Windows-only dependencies; skip on macOS
pywin32; platform_system == "Windows"
pylibCZIrw; platform_system == "Windows"

